name: Test My Action

on:
  push:
    branches: [ main ]

jobs:
  tests:
    runs-on: ubuntu-latest
    name: My First Action
    steps:
      - name: Gosta
        id: achabom
        uses: luizloja/potionOfWeakness@v7
        with: 
          file: 'gosta' 
